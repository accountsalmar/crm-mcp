# =============================================================================
# ODOO CRM MCP SERVER - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it contains secrets)
# =============================================================================

# =============================================================================
# ODOO CONNECTION (Required)
# =============================================================================
ODOO_URL=https://your-odoo-instance.com
ODOO_DB=your_database_name
ODOO_USERNAME=your_username
ODOO_PASSWORD=your_password_or_api_key

# =============================================================================
# SERVER SETTINGS
# =============================================================================
# Transport mode: "stdio" for Claude Desktop/Code, "http" for Claude.ai browser
TRANSPORT=http

# HTTP server settings (only used when TRANSPORT=http)
PORT=3000
HOST=0.0.0.0

# =============================================================================
# VECTOR DATABASE SETTINGS (Optional - for semantic search features)
# =============================================================================
# Set to "false" to disable all vector features
VECTOR_ENABLED=true

# -----------------------------------------------------------------------------
# Qdrant Vector Database
# -----------------------------------------------------------------------------
# Get free cloud instance at: https://cloud.qdrant.io/
# Or run locally: docker run -p 6333:6333 qdrant/qdrant

# For Qdrant Cloud:
QDRANT_HOST=https://your-cluster-id.region.cloud.qdrant.io:6333
QDRANT_API_KEY=your_qdrant_api_key

# For local Qdrant (no API key needed):
# QDRANT_HOST=http://localhost:6333
# QDRANT_API_KEY=

# Collection name (default: odoo_crm_leads)
QDRANT_COLLECTION=odoo_crm_leads

# -----------------------------------------------------------------------------
# Voyage AI Embeddings
# -----------------------------------------------------------------------------
# Get API key at: https://www.voyageai.com/
# Free tier includes ~$2 credits (enough for ~100K embeddings)
VOYAGE_API_KEY=pa-your_voyage_api_key_here

# Embedding model (default: voyage-3-lite, cheapest and fast)
# Options: voyage-3-lite, voyage-3, voyage-3-large
EMBEDDING_MODEL=voyage-3-lite

# Embedding dimensions (default: 512)
# voyage-3-lite supports: 256, 512 (max)
# voyage-3 supports: 256, 512, 1024
EMBEDDING_DIMENSIONS=512

# =============================================================================
# ADVANCED SETTINGS (Usually no need to change)
# =============================================================================
# Sync batch size for vector operations
# VECTOR_SYNC_BATCH_SIZE=200

# Circuit breaker settings for resilience
# VECTOR_CIRCUIT_BREAKER_THRESHOLD=3
# VECTOR_CIRCUIT_BREAKER_RESET_MS=30000
